[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m uvicorn arancel_mx.api.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
